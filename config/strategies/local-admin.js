var passport = require('passport'),
	LocalStrategy = require('passport-local').Strategy,
	User = require('mongoose').model('User'),
	constants = require('../../app/common/app.server.constants');

module.exports = function() {
};
